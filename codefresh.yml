version: '1.0'

steps:

     unit_tests:
          title: Unit Tests
          image: node:6.9.2
          commands:
            - npm install --silence
            - npm test

     build_step:
         title: Build
         type: build
         dockerfile: Dockerfile
         image_name: aloncodefresh/cf3696repoa

